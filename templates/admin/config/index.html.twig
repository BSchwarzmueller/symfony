{% block content %}
    <h1>System Konfiguration</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Speichern</button>
    {{ form_end(form) }}

    <a href="{{ path('admin.config.add') }}"><button>Neu</button></a>
{% endblock %}
